# 게임 초기값·초기화 기준

## 기준 초기값

| 항목 | 값 |
| --- | ---: |
| 코인 | 500 |
| 하트 | 5 |
| 레벨 | 1 |

## 하트 규칙

- 최대 하트는 5개다.
- 하트가 5개 미만이면 30분마다 1개씩 회복한다.
- 하트가 5개이면 타이머는 `Max`로 표시된다.

## 초기화 작업 규칙

1. 초기화는 코인 500, 하트 5, 레벨 1로만 수행한다.
2. 초기값 정의의 원본은 `godot-shell.html`이다. `web/index.html`은 Godot 웹 내보내기 산출물이므로 직접 수정하지 않는다.
3. 초기화 전에 이 문서와 `godot-shell.html`의 `INITIAL_COINS`, `MAX_HEARTS`, `STORAGE_PREFIX`를 확인한다.
4. 기존 저장 데이터만 초기화하며, 초기값·하트 회복 규칙·상점 보상 규칙을 바꾸지 않는다.
5. 초기화 뒤에는 실행 중인 게임에서 코인 500, 하트 5, 레벨 1을 확인한다.

## 별도 규칙

- 480 상품의 하트 10개 지급은 초기값과 별개의 상점 보상 규칙이다. 초기화 작업에서 이 규칙을 변경하거나 최대 하트를 바꾸지 않는다.

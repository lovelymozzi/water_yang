# Water Yang 개발 규율

이 문서는 Godot 4.x 프로젝트에서 고양이 퍼즐 프로토타입을 수정할 때 지켜야 할 기준을 기록한다. 구현을 바꾸기 전 이 문서를 확인하고, 새로운 합의가 생기면 함께 갱신한다.

## 프로젝트 기준

- 엔진과 스크립트는 Godot 4.x 및 GDScript를 기준으로 한다.
- 게임은 세로 화면에서 보이는 3D 그리드 퍼즐이다.
- 각 그리드 셀은 정사각형이며, 가로와 세로 셀 간격은 반드시 동일하다.
- 보드 전체는 레벨 크기에 따라 직사각형이 될 수 있다. 보드의 세로 여백은 넓게 둘 수 있지만, 타일의 크기와 간격은 변하면 안 된다.
- 월드 좌표 변환은 `LevelManager.grid_to_world()`를 단일 기준으로 사용한다. 노드의 Transform을 손으로 옮겨 그리드 값과 어긋나게 만들지 않는다.

## 씬과 에디터

- 메인 씬은 `scenes/main_scene.tscn`이다.
- 레벨 배치용 고양이는 `LevelManager/LayoutCats` 아래에 둔다.
- 레벨 배치용 장애물은 `LevelManager/LayoutObstacles` 아래에 둔다.
- 에디터 미리보기와 실행 결과는 같은 직렬화된 `grid_pos`, 방향, 길이를 사용해야 한다.
- 배치값을 변경한 뒤에는 `LevelManager` Inspector의 `Refresh Board Preview`를 실행해 보드와 고양이 미리보기를 갱신한다.
- 실행 중 Inspector에서 바꾼 값은 기본적으로 씬 파일에 저장되지 않는다. 영구 변경은 플레이를 멈춘 뒤 적용한다.
- 런타임에 생성되는 보드/타일/고양이 시각 노드는 에디터 레이아웃 노드와 분리해 관리한다.

## 그리드와 조작

이동 구현은 2026-08-11에 전부 삭제하고 백지에서 다시 시작했다. **세부 규칙과 수치는 `1_움직임고찰.md`가 단일 기준이다.** 여기에는 그 문서와 중복되지 않는, 구현 방식이 바뀌어도 유지되는 것만 남긴다. 삭제된 이전 구현은 git 커밋 `965d521`에 그대로 있다.

- 고양이 몸은 리드에서 반대쪽 끝까지 인접한 셀 목록(`body_cells`)으로 관리한다. **`body_cells[0]`은 언제나 드래그로 끌고 있는 쪽(리드)이다.** 모델의 얼굴이 어느 쪽인지와는 무관하다.
- 논리와 렌더를 분리한다. `body_cells`는 언제나 그리드에 딱 맞는 이산 경로이고, 충돌 판정은 항상 이것만 본다.
- 점유 조회는 `CatEntity.get_occupied_cells()` 하나만 쓴다. 표시용과 판정용이 다른 집합이 되면 걸침 규칙이 즉시 어긋난다.
- 한 번의 유효 이동은 정확히 한 그리드 셀만 이동한다. 이는 **논리(`body_cells`) 기준**이다.
- 후진은 "반대쪽 끝이 선두인 이동"이다. 선두(움직이는 끝)와 리드(손가락이 잡은 끝)는 별개다. 전이·점유·본 포즈는 선두만 알면 되므로 코드가 공유된다.
- 두 끝의 이동 규칙은 완전히 대칭이다. 양쪽에 서로 다른 코드 경로를 두지 않는다. 반대쪽 끝을 잡으면 `body_cells`를 뒤집고 `_lead_is_tail`만 토글한다.
- 이동 중 렌더는 부드럽게 이어지되, 최종 확정은 항상 그리드 단위로 끝난다.
- `grid_pos`는 에디터 레이아웃용이다. 세터가 몸을 직선으로 되돌리므로 런타임 이동 중에는 절대 대입하지 않는다.
- 몸통은 메시나 본의 비균일 스케일로 고양이 비율을 바꾸지 않는다.
- 기본 고양이는 자기 몸이 차지한 셀 묶음에 정확히 맞춰 배치한다. 양 끝 본이 양 끝 셀 중심에 온다.
- Inspector의 `Initial Length`는 그대로 셀 개수다(`5` → 5칸). 몸 길이는 최소 3칸이고 최대는 인위적으로 제한하지 않는다. 보드 크기와 유효한 빈 셀이 실제 상한이 된다.
- 드래그 중 회전 방향은 그리드 진행 방향과 일치해야 한다. 입력한 꺾임의 반대 방향으로 몸이 꼬이면 회전 부호와 본 로컬 축부터 검증한다.

### 지난 구현에서 배운 함정

구현 방식이 바뀌어도 그대로 적용되는 것만 남긴다.

- **계산한 본 자리와 실제 스켈레톤이 어긋나는 것이 이 프로젝트의 단골 실패 모드다.** 포즈를 회전 누적으로 재구성하면 계속 생긴다. "계산한 자리 vs 실제 본 위치" 대조를 회귀 검사로 두면 즉시 잡힌다. 현재 구현은 폴리라인 호 길이로 글로벌 포즈를 직접 구한 뒤 부모 순서대로 로컬 포즈로 환산한다(`_apply_bone_globals`). 누적 회전이 없으므로 어긋날 여지가 없고, `tests/movement_check.gd`가 이탈량을 계측한다.
- **`cat1.fbx`의 `Bone002`는 몸통 체인과 부모 링크가 없는 별도 루트 본이다.** 부모를 따라 걷는 방식으로는 `Bone022`까지 닿지 못한다. 이 때문에 이전 `_build_bone_chain()`은 공통 조상 검사에서 조용히 실패해 체인이 빈 배열로 남았고, 길이 신축이 아예 동작하지 않았다. 머리 본은 앞에 따로 붙여야 한다.
- 관절 간격은 부모 링크의 로컬 오프셋 길이로 재지 말고 **rest 글로벌 위치 사이의 실제 거리**로 잰다. 그래야 분기 리그와 링크가 끊긴 루트 본이 함께 맞는다.
- FBX 인스턴스는 스폰 시 한 번만 만든다. 이동마다 다시 로드하면 스켈레톤 포즈가 프레임마다 리셋되어 보간이 불가능하다.
- 소수 성분의 부호는 `signi()` 로 내지 않는다. `signi()` 는 int 를 받으므로 `signi(-0.55)` 가 0이 되어 셀 사이 추종이 통째로 멈춘다.
- 진행 축은 방향(각도)이 아니라 그리드 좌표로 판단한다. 각도로 고르면 칸을 확정한 직후 진행축 성분이 0이 되는 순간마다 측면 성분이 이겨서, 포인터가 같은 열 안에 있는데도 머리가 옆 열로 꺾인다.
- 잡은 순간 포인터가 셀 중심에서 빗겨 있던 양은 이후 좌표에서 계속 빼 준다. 이 보정이 없으면 빗겨 잡은 만큼이 영구 편향으로 남는다.

## 고양이 모델과 리그

- 게임에서 사용하는 모델은 `res://water_yang/cat1.fbx`다.
- 기본 텍스처는 `res://water_yang/cat1.jpeg`다.
- 원본 DCC 파일인 `water_yang/cat1.max`와 `water_yang/cat1.psd`는 재임포트/수정을 위한 소스 에셋으로 함께 유지한다.
- 고양이 얼굴은 어떤 배치, 이동, 회전 상태에서도 하늘(+Y)을 향해야 한다. 바닥을 향하도록 모델이나 루트 Transform을 뒤집지 않는다.
- 고양이의 본 체인은 뱀처럼 꺾이는 이동을 위한 의도된 구조다. 부모-자식 연결을 임의로 끊지 않는다.
- 머리→꼬리 경로는 `Bone002`(별도 루트) 다음에 `Bone003 → Bone004 → … → Bone022`가 이어진다. `Bone001`은 이 경로에 없다. 앞발(`Bone031`, `Bone033`)의 부모이며 `Bone003`과 같은 위치에 있다.
- `Bone001`, `Bone002`, `Bone017`에는 어떤 상황에서도 스케일 값을 적용하지 않는다.
- 길이 증감에는 `Bone007`부터 `Bone014`까지의 관절 간격만 늘린다(`STRETCH_BONE_FIRST`/`LAST`). 모든 본의 포즈 스케일은 항상 `(1, 1, 1)`로 유지한다.
- 본 포즈로 길이와 방향을 표현하되, 고양이 전체 노드의 스케일을 이동 중 변경하지 않는다.
- 모델의 기준 크기는 타일 크기와 연동한다. 현재 `fbx_scale_per_tile` 기본값은 `7.65`이며, 변경 시 4칸 배치와 몸 비율을 함께 확인한다.
- `cat1.fbx`는 분기형 리그다. 게임에서 사용하는 본 경로는 `Bone002`(머리 끝)에서 `Bone022`(꼬리 끝)까지이며, `cat_entity.gd`의 `_build_bone_chain()`이 구성한다.
- 관절 간격은 균일하지 않다. 따라서 경로 위 위치는 본 인덱스 비율이 아니라 rest 누적 거리로 환산해야 한다.
- 코너 회전은 여러 관절에 나눠 주지 않는다. 나눠 주면 그만큼 코너가 잘려 몸통이 옆 칸으로 밀린다.
- 본 체인 길이는 **메시 기준**으로 맞춘다. 발자국(N칸)에서 양끝 여백(`footprint_margin_cells`)과 메시 오버행을 뺀 값이다. 메시가 양끝 본보다 내미는 양은 머리 0.41칸, 꼬리 0.10칸으로 4배 차이나므로 이걸 무시하면 꼬리쪽만 비어 보인다.
- 폴리라인 양끝 내밀기는 음수가 되면 경로가 접혀 호 길이가 늘어난다. 반드시 0으로 묶는다.

## 구멍

- 구멍은 정확히 한 칸이다. 배치는 `LevelManager/LayoutHoles` 아래의 `HoleMarker`(`grid_pos`)로만 하며, 장애물 마커와 같은 방식이다.
- 구멍 칸은 `CellState.HOLE`이고 `is_cell_blocked_for()`에서 차단으로 본다. **구멍은 경로가 아니다.** 브릿지가 구멍을 지나가면 인접 판정보다 먼저 밟게 된다.
- 흡입 판정은 **셀 중앙에서 커밋하는 순간에만** 한다(`_finish_step()`). 전이 중에 시작하면 레일과 점유가 어긋난다.
- **색은 1:1 페어다.** 고양이(`CatEntity.color_id`)와 구멍(`HoleMarker.color_id`)이 같은 색일 때만 흡입이 걸린다. 색이 다르면 옆칸에 서 있어도 아무 일도 없다. `color_id = -1`은 아무 색과도 짝이 되는 와일드카드다.
- 색 팔레트는 `LevelManager.pair_colors` 하나뿐이고 배열 인덱스가 곧 `color_id`다. 짝 판정과 표시색이 갈라지지 않게 고양이 틴트와 구멍 비주얼이 모두 `get_pair_color()`에서 색을 받는다. 색 비교는 `color_ids_pair()`, 인접 조회는 `adjacent_hole(cell, color_id)` 하나만 쓴다.
- 구멍 비주얼은 테두리가 짝 색이고 안쪽 구덩이는 그 색을 어둡게 한 값이다. 와일드카드 구멍만 `hole_rim_color`/`hole_pit_color`를 쓴다.
- 고양이 틴트는 `tint_from_pair_color`를 켜면 팔레트 색에서 만들고(그라디언트 양끝은 그 색의 명암), 끄면 Inspector에 적어 둔 `tint_color`/그라디언트 값을 그대로 쓴다. 손으로 맞춘 색과 팔레트 색이 어긋나면 플레이어가 짝을 볼 수 없으므로, 색을 여러 개 쓰는 레벨에서는 켠다.
- 판정 대상은 몸의 두 끝이며 4방향 인접만 본다. 대각선은 걸리지 않는다. 두 끝을 모두 보는 이유는 후진에서 후미가 리드가 가지 않은 칸으로 나갈 수 있기 때문이다(벽 타기). 조작상 양끝이 동시에 인접하는 경우는 없고, 동시에 걸리면 리드가 먼저다.
- 구멍에 들어간 고양이는 **탈출로 취급한다.** `on_cat_escaped()`를 그대로 쓰므로 마지막 고양이가 들어가면 `level_cleared`가 나온다.
- 흡입 연출은 포즈 경로를 새로 만들지 않는다. 기존 폴리라인 호 길이 샘플링에 "밀려 들어간 길이"(`_swallowed_arc`) 하나만 더해, 끝점을 지난 부분을 구멍 중심까지 보낸 뒤 아래로 내린다. 누적 회전이 없어야 한다는 원칙은 흡입에서도 같다.
- 바닥 아래로 내려간 부분은 알파 블렌딩이 아니라 셰이더의 디더 `discard`로 지운다(`sink_clip_*`). 평소 불투명 렌더 경로와 아웃라인 정렬을 건드리지 않기 위해서다. 본체와 아웃라인은 **같은 값**을 써야 껍데기만 남지 않는다.
- 흡입이 시작되면 점유를 즉시 놓고 조작을 받지 않는다. `DragController`는 흡입 중인 고양이를 잡지 않으며, 잡고 있던 고양이가 흡입되면 터치를 끝낸다.
- 위에서 내려다보는 직교 카메라라 깊이 단서가 없다. 구멍 비주얼은 테두리(타일 자리) + 그 안쪽의 어두운 면이며, 어두운 면은 테두리보다 **위**에 얹어야 보인다.
- 레벨 시작 시점부터 이미 인접해 있는 배치는 흡입하지 않는다. 판정이 이동 커밋에만 걸려 있기 때문이며, 그런 배치는 레벨 설계 오류로 본다.

## 머티리얼, 조명, 그림자

- 고양이에는 툰 셰이더와 아웃라인 셰이더를 적용한다.
- 고양이의 색상 변형은 텍스처를 교체하지 않고 `tint_color`를 우선 사용한다.
- 바닥은 조명의 영향을 받고 고양이 그림자를 받아야 한다. Unshaded 머티리얼이나 뒤집힌 노멀을 바닥에 적용하지 않는다.
- 그림자 방향은 `DirectionalLight3D`의 회전으로 결정된다. 고양이와 바닥에서 그림자 방향이 서로 반대라면 바닥 노멀, 메시 면 방향, 셰이더의 법선 처리부터 점검한다.
- 그림자 품질 관련 값은 메인 씬의 `DirectionalLight3D`에 저장한다. 실행 중 값이 좋아 보여도 플레이 종료 후 유지하려면 씬 편집 상태에서 설정해야 한다.

## 진단

- 파일 로깅은 `project.godot` 의 `[debug] file_logging` 으로 켜 둔다. macOS 로그 위치는 `~/Library/Application Support/Godot/app_userdata/LeonOutPrototype/logs/` 이고, 실행 시 `[boot]` 줄에 경로가 찍힌다.
- 0.25초를 넘긴 프레임은 `main_scene.gd` 가 `[stall]` 경고로 남긴다.
- 프리즈를 보고할 때는 로그 파일과 함께 (1) 에디터인지 실행 게임인지, (2) 직전 조작, (3) 몸 길이 설정을 함께 남긴다.

## 검증과 변경 원칙

- 리그, 포즈, 셰이더, 조명 변경 뒤에는 에디터 뷰와 게임 실행 뷰를 모두 확인한다.
- 본 변경 뒤에는 다음을 반드시 확인한다: 얼굴이 위를 보는지, 4칸 중앙 정렬인지, 몸 비율이 원본과 같은지, 스케일 잠금 본이 변하지 않는지.
- 시각적으로 확인하지 못한 기능은 완료되었다고 표현하지 않는다.
- 실험용/사용하지 않는 포즈 구현은 검증된 구현으로 대체된 뒤 정리한다.
- 수동 코드 변경은 `apply_patch`로 적용한다.
- 사용자 또는 자동 생성 변경사항을 임의로 되돌리지 않는다.
- 의미 있는 변경 단위마다 Git 커밋을 만들고 원격 `main`에 푸시한다.

## 현재 상태

- 전진 이동을 2026-08-11에 새로 구현했다. 스펙은 `1_움직임고찰.md`다.
  - `scripts/drag_controller.gd` — 터치/마우스 드래그, 양 끝 그랩, 홀드, 강제 릴리즈.
  - `scripts/cat_entity.gd` — 경로 큐, BFS 브릿지, 셀 단위 커밋, 폴리라인 본 포즈.
  - `scripts/level_manager.gd` — 걸침 포함 점유, 차단 조회, 점유 칸 하얀 음영.
- 후진도 구현했다(`1_움직임고찰.md` 7절). 몸이 자기 모양을 따라 밀리고, 후미가 막히면 벽을 타고 흐른다.
- 1칸 구멍과 흡입도 구현했다. 두 끝 중 하나가 구멍 옆칸에 들어선 순간 그 끝부터 빨려 들어가 사라지고, 탈출로 계산된다. 테스트 레벨의 구멍은 `(5, 2)`이고 색은 `0`이다.
- 색 페어 규칙을 넣었다. `color_id`가 같은 고양이만 그 구멍으로 빠진다. `tests/hole_check.gd`의 `_check_color_pair()`가 색이 다를 때 흡입되지 않는 것까지 검사한다.
- 회귀 검사: `Godot --headless --script tests/movement_check.gd` → `MOVEMENT CHECK: PASS`, `Godot --headless --script tests/hole_check.gd` → `HOLE CHECK: PASS`. 이동 검사는 고양이를 보드 곳곳으로 순간이동시키므로 시작 시 구멍을 지운다. 두 하네스는 서로 독립이어야 한다.
- 시각 확인: `Godot --script tests/capture_shots.gd` → `user://shots/*.png`, `Godot --script tests/capture_hole.gd` → `user://shots_hole/*.png`. 헤드리스가 아니어야 렌더된다.
- 남은 시각 이슈: 90도 코너가 머리 청크 안에 들어오는 순간 목·어깨가 눌린다. 코너 회전을 관절에 분산하거나 코너를 둥글게 깎으면 완화되지만, 둘 다 "본은 셀 중앙만 관통" 규칙과 충돌한다. 결정 전까지 그대로 둔다.

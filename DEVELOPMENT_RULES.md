# Water Yang 개발 규율

이 문서는 Godot 4.x 프로젝트에서 고양이 퍼즐 프로토타입을 수정할 때 지켜야 할 기준을 기록한다. 구현을 바꾸기 전 이 문서를 확인하고, 새로운 합의가 생기면 함께 갱신한다.

## 프로젝트 기준

- 엔진과 스크립트는 Godot 4.x 및 GDScript를 기준으로 한다.
- 게임은 세로 화면에서 보이는 3D 그리드 퍼즐이다.
- 각 그리드 셀은 정사각형이며, 가로와 세로 셀 간격은 반드시 동일하다.
- 보드 전체는 레벨 크기에 따라 직사각형이 될 수 있다. 보드의 세로 여백은 넓게 둘 수 있지만, 타일의 크기와 간격은 변하면 안 된다.
- 월드 좌표 변환은 `LevelManager.grid_to_world()`를 단일 기준으로 사용한다. 노드의 Transform을 손으로 옮겨 그리드 값과 어긋나게 만들지 않는다.

## 씬과 에디터

- 메인 씬은 `scenes/main_scene.tscn`이다.
- 레벨 배치용 고양이는 `LevelManager/LayoutCats` 아래에 둔다.
- 레벨 배치용 장애물은 `LevelManager/LayoutObstacles` 아래에 둔다.
- 에디터 미리보기와 실행 결과는 같은 직렬화된 `grid_pos`, 방향, 길이를 사용해야 한다.
- 배치값을 변경한 뒤에는 `LevelManager` Inspector의 `Refresh Board Preview`를 실행해 보드와 고양이 미리보기를 갱신한다.
- 실행 중 Inspector에서 바꾼 값은 기본적으로 씬 파일에 저장되지 않는다. 영구 변경은 플레이를 멈춘 뒤 적용한다.
- 런타임에 생성되는 보드/타일/고양이 시각 노드는 에디터 레이아웃 노드와 분리해 관리한다.

## 그리드와 조작

- 고양이 몸은 머리에서 꼬리까지 인접한 셀 목록(`body_cells`)으로 관리한다.
- 머리와 꼬리 핸들만 드래그할 수 있다.
- 한 번의 유효 이동은 정확히 한 그리드 셀만 이동한다. 이는 **논리(`body_cells`) 기준**이다.
- 논리와 렌더를 분리한다. `body_cells`는 언제나 그리드에 딱 맞는 이산 경로이고, 점유·충돌 판정은 항상 이것만 본다.
- **드래그 중 몸은 셀 사이 중간 어디에나 있을 수 있다.** 잡은 끝은 손가락 위치를 그대로 따라가며(`_slide_cell` / `_slide_t`), `body_cells` 확정은 그 끝이 이웃 셀 중심에 닿는 순간에만 일어난다. 손가락을 뗄 때 절반을 넘었으면 그 칸으로 확정하고, 아니면 원래 칸으로 되돌린다.
- 몸 전체는 하나의 레일(`_build_track()`) 위를 호 길이로 미끄러진다. 각 관절은 자기 rest 호 길이 지점의 접선을 따르므로 몸통과 다리가 머리가 지나간 궤적을 그대로 훑는다.
- 레일에는 현재 점유 셀과 **지금 미끄러져 들어가는 중인 한 칸까지만** 넣는다. 기억한 길(`_front_trail` / `_back_trail`) 전체를 넣으면 꼬리 끝이 점유하지 않은 칸으로 삐져나간다.
- 이동 중에는 코너를 원호로 깎아 대각선 최단거리처럼 흐르게 하고(`corner_round`), 손을 놓으면 각진 그리드 형태로 되돌린다(`settle_time`).
- 돌출분은 라운딩 전 격자 길이가 아니라 **지금 렌더되는 레일 길이**로 계산해 양 끝에 절반씩 나눈다. 필렛은 경로를 반지름당 0.429씩 줄이는데, 이를 무시하면 그 차이가 전부 꼬리로 몰려 타일 밖으로 밀려난다. `_max_corner_radius()`가 코너 개수에 따라 반지름을 추가로 제한한다.
- 잡은 순간 손가락이 셀 중심에서 빗겨 있던 양(`_grab_offset`)을 이후 좌표에서 계속 빼 준다. 이 보정이 없으면 빗겨 잡은 만큼이 영구 편향으로 남아, 칸 확정 직후 진행축 성분이 0이 되는 순간마다 옆칸으로 튀어나간다.
- **축 선택은 방향(각도)이 아니라 그리드 좌표로 판단한다.** 각도(`to_pointer.normalized().dot(axis)`)로 고르면 칸을 확정한 직후 진행축 성분이 0이 되는 순간마다 측면 성분이 이겨서, 마우스가 같은 열 안에 있는데도 머리가 옆 열로 꺾인다.
- 진행 중인 축은 유지하고, 측면 축은 **마우스가 그 방향 셀 경계를 실제로 넘었을 때만**(`SLIDE_AXIS_SWITCH` = 타일 반폭) 후보가 된다. 이력(`SLIDE_AXIS_HYSTERESIS`)은 보조다.
- 드래그 시작 축은 몸통이 지금 향한 선으로 둔다. 축이 비어 있으면 첫 프레임의 진행 성분이 0이라 조준용 미세한 좌우 움직임이 축을 가로챈다.
- 축이 바뀌는 지점에서는 그 순간의 진행량을 기준점(`_axis_entry`)으로 잡아 0부터 다시 센다. 이 보정이 없으면 전환 지점에서 머리가 반 칸까지 순간이동한다.
- 소수 성분의 부호는 `signi()` 로 내지 않는다. `signi()` 는 int 를 받으므로 `signi(-0.55)` 가 0이 되어 셀 사이 추종이 통째로 멈춘다. `_unit_sign()` 을 쓴다.
- 본 방향은 관절 위치가 아니라 **그 관절이 담당하는 구간의 중간**에서 접선을 읽는다(`_bone_span_direction`). 관절 위치에서 읽으면 코너가 "가장 가까운 관절"이 아니라 "코너 다음 관절"에 실려 꺾임이 뭉개진다.
- 정지 상태에서는 접선(예각), 이동 중에는 구간 양 끝을 잇는 현(연속)을 쓴다(`_smooth_weight`). 접선은 세그먼트 단위로 계단식이라 몸이 미끄러질 때 관절 방향이 5도씩 툭툭 튀며 떨린다. 예각이 필요한 건 멈춰 있을 때뿐이므로 둘을 이 가중치로 섞는다.
- 머리가 향한 방향(`_forward`)은 레일에서 바로 읽지 않고 시간축으로 따라가게 한다. 레일은 슬라이드가 시작되는 순간 머리 셀에서 꺾이므로, 그대로 쓰면 코가 한 프레임에 한 칸 가까이 순간이동한다. 머리 셀 지점은 레일 위에 정확히 두고 코 끝만 `_forward` 방향으로 돌출분만큼 내민다.
- FBX 인스턴스는 스폰 시 한 번만 만든다. 이동마다 다시 로드하면 스켈레톤 포즈가 프레임마다 리셋되어 보간이 불가능하다.
- 이동 판정과 확정은 분리한다. `_plan_endpoint_move()`가 가능 여부와 결과를 계산하고 `_commit_endpoint_move()`가 반영한다. 드래그가 그 칸으로 미끄러져도 되는지는 `can_move_endpoint()`로 미리 본다.
- 드래그해서 끌고 있는 쪽이 곧 "머리"다. 실제 모델의 얼굴이 어느 쪽인지와는 무관하며, 두 끝의 이동 규칙은 완전히 대칭이다.
- **전진**: 잡은 끝이 새 칸으로 나아가면 반대쪽 끝이 그 경로를 그대로 따라온다.
- **후진**: 잡은 끝을 몸통 안쪽으로 밀면, 반대쪽 끝은 자기가 지나온 길(`_front_trail` / `_back_trail`)을 기억해 되짚어 간다. 기억이 남아 있지 않은 칸으로 나가야 하는 순간부터는 미는 힘 그대로 직진하는 자연 후진으로 바뀐다.
- 지나온 길 기록은 이동이 확정된 뒤에만 갱신한다. 잡은 끝이 새 방향으로 틀면 그쪽 기록은 버리고, 기억한 칸을 되짚어 가면 그만큼만 소비한다.
- 몸통을 새로 배치하면(`_reset_straight_body()`) 양쪽 기록도 함께 비운다.
- 이동 후보 셀은 보드 안에 있고, 장애물이나 다른 고양이가 점유하지 않아야 한다.
- 꺾임 이동의 논리 점유는 몸통 경로 셀만 사용하며, 이로 인해 정상적인 인접 셀 이동이 막히면 안 된다. 기본 꺾임은 하나의 그리드 관절에서 즉시 90°로 처리해 몸통이 대각 칸을 가로지르지 않으며, 메시나 본의 비균일 스케일로 고양이 비율을 바꾸지 않는다.
- 기본 고양이는 자기 몸이 차지한 셀 묶음의 정중앙에 맞춰 배치한다.
- `cat1.fbx`의 rest 체인 길이는 고정이라 현재 스케일(7.65)에서 약 3.75칸까지만 닿는다. 그래서 `initial_length` 기본값은 4다. 이동 로직 자체는 길이와 무관하게 동작하지만, 4보다 길게 두면 논리 점유 셀은 맞아도 뒤쪽 셀이 시각적으로 빈다. 길이를 늘리려면 모델 대응 방식(몸통축 스트레치 또는 세그먼트 반복)을 먼저 정해야 한다.
- 몸 길이는 최소 2칸부터 변경 가능하게 설계하고, 최대 길이는 추후 레벨 디자인에 맞춰 정한다. 현재 Inspector 기본 최대값은 16이다.
- 드래그 중 회전 방향은 그리드 진행 방향과 일치해야 한다. 입력한 꺾임의 반대 방향으로 몸이 꼬이면 회전 부호와 본 로컬 축부터 검증한다.

## 고양이 모델과 리그

- 게임에서 사용하는 모델은 `res://water_yang/cat1.fbx`다.
- 기본 텍스처는 `res://water_yang/cat1.jpeg`다.
- 원본 DCC 파일인 `water_yang/cat1.max`와 `water_yang/cat1.psd`는 재임포트/수정을 위한 소스 에셋으로 함께 유지한다.
- 고양이 얼굴은 어떤 배치, 이동, 회전 상태에서도 하늘(+Y)을 향해야 한다. 바닥을 향하도록 모델이나 루트 Transform을 뒤집지 않는다.
- 고양이의 본 체인은 뱀처럼 꺾이는 이동을 위한 의도된 구조다. 부모-자식 연결을 임의로 끊지 않는다.
- `Bone003`은 목이 아니라 몸통 시작 본으로 취급한다.
- `Bone001`, `Bone002`, `Bone017`에는 어떤 상황에서도 스케일 값을 적용하지 않는다.
- 본 포즈로 길이와 방향을 표현하되, 고양이 전체 노드의 스케일을 드래그 중 변경하지 않는다.
- 모델의 기준 크기는 타일 크기와 연동한다. 현재 `fbx_scale_per_tile` 기본값은 `7.65`이며, 변경 시 4칸 배치와 몸 비율을 함께 확인한다.
- `cat1.fbx`의 본 체인은 `Bone001`(머리, 인덱스 0)부터 `Bone018`(꼬리 끝, 인덱스 17)까지 인덱스 순서 그대로 이어진 단일 체인이다. 순서를 상수로 적어두지 않고 `cat_entity.gd`의 `_build_bone_chain()`이 부모-자식 링크를 따라 매번 계산한다.
- 관절 간격은 균일하지 않다(머리 쪽 `Bone002`와 꼬리 끝 `Bone018`이 특히 길다). 따라서 경로 위 위치는 본 인덱스 비율이 아니라 rest 누적 거리로 환산해야 한다.
- 코너 회전은 코너 셀까지의 거리(머리 돌출분 + 셀 개수 x 타일 크기)에 가장 가까운 관절 하나에 준다. 여러 관절에 나눠 주면 그만큼 코너가 잘려 몸통이 옆 칸으로 밀린다. 조정값은 `CORNER_SMOOTH_TILES`다.
- 본 체인의 rest 길이가 셀 중심 경로보다 긴 만큼은 머리와 꼬리에 절반씩 내민다(`_body_overhang()`). 이 규칙 덕분에 몸 길이가 몇 칸이든 셀 묶음의 정중앙에 놓인다.

## 머티리얼, 조명, 그림자

- 고양이에는 툰 셰이더와 아웃라인 셰이더를 적용한다.
- 고양이의 색상 변형은 텍스처를 교체하지 않고 `tint_color`를 우선 사용한다.
- 바닥은 조명의 영향을 받고 고양이 그림자를 받아야 한다. Unshaded 머티리얼이나 뒤집힌 노멀을 바닥에 적용하지 않는다.
- 그림자 방향은 `DirectionalLight3D`의 회전으로 결정된다. 고양이와 바닥에서 그림자 방향이 서로 반대라면 바닥 노멀, 메시 면 방향, 셰이더의 법선 처리부터 점검한다.
- 그림자 품질 관련 값은 메인 씬의 `DirectionalLight3D`에 저장한다. 실행 중 값이 좋아 보여도 플레이 종료 후 유지하려면 씬 편집 상태에서 설정해야 한다.

## 검증과 변경 원칙

- 리그, 포즈, 셰이더, 조명 변경 뒤에는 에디터 뷰와 게임 실행 뷰를 모두 확인한다.
- 본 변경 뒤에는 다음을 반드시 확인한다: 얼굴이 위를 보는지, 4칸 중앙 정렬인지, 몸 비율이 원본과 같은지, 머리/꼬리 드래그 모두에서 체인 이동하는지, 스케일 잠금 본이 변하지 않는지.
- 시각적으로 확인하지 못한 기능은 완료되었다고 표현하지 않는다.
- 실험용/사용하지 않는 포즈 구현은 검증된 구현으로 대체된 뒤 정리한다.
- 수동 코드 변경은 `apply_patch`로 적용한다.
- 사용자 또는 자동 생성 변경사항을 임의로 되돌리지 않는다.
- 의미 있는 변경 단위마다 Git 커밋을 만들고 원격 `main`에 푸시한다.

## 현재 상태

- 스킨드 고양이 체인 이동은 프로토타입 단계다.
- 머리/꼬리 드래그와 본 포즈 코드는 존재하지만, 모든 꺾임 상황에서의 시각적 안정성은 아직 완전히 검증되지 않았다.
- 이후 작업은 이 문서의 확정 규칙을 지키면서 체인 포즈의 꼬임과 비율 문제를 우선 해결한다.

# 이동 스펙

2026-08-11 확정. 이전 구현은 전부 삭제했고 이 문서가 새 구현의 기준이다.

## 0. 용어

- **리드(lead)**: 드래그로 끌고 있는 쪽 끝. 이미지상 머리인지 꼬리인지와 무관하다.
- **레일(rail)**: 몸이 놓인 셀 목록. 리드가 앞(index 0), 반대쪽 끝이 뒤다.
- **큐(queue)**: 앞으로 리드가 들어갈 셀 목록. 손가락 입력이 여기에 쌓인다.
- **전이(transition)**: 리드가 다음 셀로 넘어가는 동안. 이때만 점유 칸이 1칸 늘어난다.

## 1. 수치

| 값 | 기본값 | 근거 |
| --- | --- | --- |
| 최대 이동 속도 | 8칸/초 | 보드 폭 7칸, 한 칸 약 90px. 보통 드래그(7~13칸/s)는 거의 따라붙고 빠른 플릭에서만 지연이 생긴다. |
| 큐 상한 | 4칸 | `8칸/초 × 0.5초`. **손가락이 앵커에서 이 거리 안에 있어야 입력을 받는다.** 빠른 플릭으로 멀리 튄 목표를 자동 주행으로 만들지 않기 위한 가드다. |
| 우회 큐 상한 | 12칸 | 손가락은 큐 상한 안에 있는데 벽이 두꺼워 길이 그보다 길어질 때만 여기까지 늘어난다. 벽 두께에 상한을 두지 않기 위한 값이다. |
| 그랩 반경 | 1.2칸 | 끝 셀 중심 기준. 드래그 실패를 줄이기 위해 넉넉하게 잡는다. |
| 강제 릴리즈 거리 | 2칸 | 체비셰프 거리. |

셋 다 인스펙터에서 조정 가능하다. 속도를 바꾸면 큐 상한도 `속도 × 0.5`로 함께 바꾼다.

## 2. 입력

- 두 끝 모두 잡을 수 있고 **두 끝의 로직은 완전히 동일**하다. 잡은 쪽이 리드가 된다.
- 잡는 순간 포인터가 끝 셀 중심에서 빗겨 있던 양을 기록하고, 이후 좌표에서 계속 빼 준다.
- **한 번 잡으면 터치가 끝날 때까지 그 끝을 유지한다.** 드래그 중에는 다른 끝으로 넘어가지 않는다.
- 새 터치가 시작되면 잔여 큐를 버린다. 진행 중이던 한 칸 전이만 끝내고 새 입력을 따른다.
- 반대쪽 끝으로 리드를 바꾸는 것은 전이가 끝난 시점에 적용한다. 전이 도중에 레일을 뒤집지 않는다.

### 강제 릴리즈

release 이벤트에만 의존하면 입력이 영구 고착된다. 아래를 모두 릴리즈로 처리한다.

- 터치/마우스 릴리즈, 터치 취소
- 앱 포커스 상실, 창 밖 이탈
- **막힌 상태에서 손가락이 리드 셀에서 체비셰프 2칸 이상 멀어짐** — 별도 알림 없이 조용히 종료한다.

## 3. 경로 생성

큐에 셀을 넣는 규칙은 하나다.

> 손가락이 가리키는 셀을 목표로 삼는다. 목표가 큐 끝(앵커)에 인접하면 그대로 한 칸 추가한다.
> 인접이 끊겼으면(벽 통과, 빠른 드래그로 인한 셀 건너뜀) **BFS 브릿지**로 앵커에서 목표까지 이어 붙인다.
> 브릿지가 불가능하거나 우회 큐 상한을 넘으면 추가하지 않는다.

- 브릿지는 벽을 넘지 않으므로 지름길이 생기지 않는다. 자동 우회와 같은 메커니즘이다.
- 인접한 목표는 브릿지 길이가 1이라 손가락이 그린 궤적이 그대로 보존된다. 탐색이 곡선을 직선으로 펴지 않는다.
- **거리 판정과 경로 길이는 다른 값을 쓴다.** 목표가 앵커에서 큐 상한보다 멀면 아무것도 넣지 않는다(플릭 가드). 그 안에 있으면 경로는 우회 큐 상한까지 길어질 수 있다.
  - 이렇게 나눈 이유: 두 값을 하나로 묶어 두면 벽 두께가 2칸을 넘는 순간 "손가락이 바로 옆인데도 우회를 못 하는" 상태가 된다. 한 값으로는 플릭 가드와 우회 허용을 동시에 만족시킬 수 없다.
- 큐가 꽉 차 입력을 못 받은 구간도 같은 브릿지로 잇는다. 별도 재개 조건이 필요 없다.

### 브릿지 탐색 규칙

- 상태는 `(셀, 스텝)`이다. 스텝 `k`에 도착하는 셀은 "큐를 다 소비한 뒤 k번째"를 뜻한다.
- 자기 몸은 **시간 전개**로 판정한다. 큐 소비 후 몸이 `future[0..L-1]`일 때, 스텝 `k`에서 막히는 것은 `future[i]` 중 `i + k <= L - 1` 인 것뿐이다. 뒤쪽 `k`칸은 이미 비켜났다.
- 이웃 확장 순서는 **직전 진행 방향 우선**이다. 동일 코스트 경로가 여러 개일 때 경로가 좌우로 펄럭이지 않게 하는 결정성 규칙이다.
- 이미 큐에 들어간 구간은 재계산하지 않는다. 브릿지는 언제나 큐 뒤에 덧붙는다.
- 목표가 자기 몸 위이거나 앵커와 같으면 아무것도 하지 않는다.

## 4. 이동과 충돌

- 논리 이동 단위는 정확히 한 셀이다. 렌더는 연속이지만 확정은 항상 셀 중앙에서 끝난다.
- **판정 시점은 셀 중앙에서 다음 셀을 커밋하는 순간뿐이다.** 전이가 시작되면 반드시 끝까지 간다. 0.5칸 진입 후에 판정하면 이미 겹친 뒤다.
- 커밋 조건: 보드 안 + 장애물 아님 + 다른 고양이 아님 + **자기 몸 셀이 아님(뒤끝 칸 포함)**.
- 뒤끝 칸 진입을 허용하지 않는다. 전이 중 뒤끝은 아직 0.5칸을 점유하고 있으므로 들어가면 겹친다.
  - 결과: 사방이 막힌 2×2 공간에서는 회전할 수 없다. 순간적으로 5칸이 되어야 하기 때문이다.
  - 열린 보드에서의 U턴은 항상 가능하다. 뒤끝이 비켜나므로 2칸 폭이면 돌 수 있다.
- 몸 길이는 고정이다. 따라서 **자기 몸에 막힌 상태는 자력으로 풀리지 않는다.** 대기 후 재시도 같은 것은 논리적으로 성립하지 않으므로 두지 않는다. 막히면 큐를 버리고 정지한다.
- 탈출 수단은 손을 떼고 반대쪽 끝을 잡는 것이다.

## 5. 점유

- 조금이라도 걸쳐 있으면 점유다. 전이 중에는 점유 칸이 몸 길이보다 1칸 많다(4칸 고양이 → 5칸).
- 점유 칸에는 하얀색 음영을 표시한다.
- 표시용 집합과 판정용 집합은 같다. 둘 다 걸침을 포함한 레일 전체를 쓴다.

## 6. 포즈

- 몸은 리드 끝점 → 반대쪽 끝점을 잇는 **셀 중심 폴리라인**이다. 본은 그리드 중앙만 관통한다.
- 본 위치는 폴리라인의 **호 길이(arc length)로 직접 계산**한다. 회전을 누적해 포즈를 재구성하지 않는다. 이 프로젝트의 단골 실패 모드다.
- 호 길이 환산은 본 인덱스 비율이 아니라 rest 누적 거리를 쓴다. 관절 간격이 균일하지 않다.
- 길이 신축은 `Bone007~Bone014`의 누적 거리에만 배율을 준다. 모든 본의 포즈 스케일은 `(1,1,1)`로 유지한다.
- 각 본의 방향은 자기 위치가 놓인 폴리라인 선분의 방향이다. 코너 회전을 여러 관절에 나눠 주지 않는다.
- 정지 자세와 이동 자세는 같은 함수를 쓴다. 정지는 폴리라인이 직선인 경우일 뿐이다.
- 몸은 발자국(N칸)을 채우되 양끝에 `footprint_margin_cells`(기본 0.1칸)만큼 여백을 둔다. 메시가 양끝 본보다 더 내미는 양(머리 0.41칸, 꼬리 0.10칸)이 앞뒤로 다르므로 그만큼 빼서 폴리라인 양끝을 내민다.

## 7. 후진

후진은 리드를 뒤로 미는 것이다. 몸 전체가 **자기 현재 모양을 따라 한 칸 밀린다.**

- 트리거: 손가락이 자기 몸 위 칸을 가리키면 후진이다. 리드에서 몇 번째 칸인지가 곧 밀어 넣을 스텝 수이며 큐 상한으로 자른다.
- 몸에 남아 있는 굽이는 그대로 따라간다. 앞 칸들이 뒤 칸 자리로 미끄러지므로 별도 처리가 필요 없다.
- **예전 경로는 기억하지 않는다.** 몸을 벗어난 구간에서 후미는 마지막 진행 방향으로 공직진한다. 상태는 `body_cells` 하나뿐이다.
- 새로 점유하는 칸은 후미 앞 한 칸뿐이다. 리드가 자기 몸 칸으로 들어가는 것은 강체 슬라이드라 충돌이 아니므로 `can_enter()`(자기 몸 금지)를 쓰지 않는다.
- 방향이 바뀌면(전진 ↔ 후진) 반대쪽 큐를 버린다. 진행 중이던 한 칸 전이만 끝낸다.

### 벽 슬라이드

후미 정면이 막히면 좌우로 흐른다. 보드 밖도 벽으로 본다.

- 양쪽 다 열려 있으면 **무작위**로 고른다. 한쪽만 열려 있으면 그쪽으로 간다.
- 무작위는 **슬라이드가 새로 시작될 때만** 굴린다. 한 번 옆으로 꺾이면 그 방향이 새 후미 방향이 되므로, 다음 스텝부터는 "직진"이 곧 벽을 타고 흐르는 것이 된다. 그래서 벽을 따라가다 지그재그로 떠는 일이 없고, 모서리를 만나면 자연스럽게 돌아 나간다.
- 세 방향이 모두 막히면 큐를 버리고 정지한다.

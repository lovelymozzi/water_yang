
각 아이템은 3개가 기본으로 제공되며,  사용 할때마다 1개 차감하면돼.  성공적으로 사용 했을때만 1개 차감 하도록 해줘. 

아이템을 선택시 item_cancle를 갯수 바인딩키 위에 올려주어 아이템 실행 취소를 할 수 있도록 함.


## 아이템1
item1=item.remove
    obstacle_tile_1x1.tscn 위에 item1-png-387를 UI 형태로 띄워서 사용자가 item1-png-387를 클릭하면 그 하단에 있는 obstacle_tile_1x1.tscn가 
    
    아이템을 클릭하면 모든 obstacle_tile_1x1.tscn 위에 item1-png-387이미지가 올려져 있는 상태가 되고, 유저가 그중 1개를 클릭하면  item1-png-387이미지는 모두 제거 되고,  item1-png-387이 선택된 곳에 배치된 obstacle_tile_1x1.tscn 하나가 제거되는 구조
    
    아이템을 사용해서  유저가  item1-png-387이미지를 선택하면 obstacle_tile_1x1는 는 녹색 파티클을 터트리며 사라진다. 


    사운드는 diggingt_optimized.MP3를 사용

    item1-png-387 이미지는 사이즈 애니메이션으로 뽀잉뽀잉 움직임을 주면서 야간 위, 아래로 동동거리는 모션을 준다.






## 아이템2
item2=itemitem.timestop.current: 인게임 타이머가 10초간 멈춤

아이템2가 발동되면 시각 효과로 가장 자리에  디졸브 쉐이더로 얼음이 얼었다가, 10초가 지나면 디졸브 효과로 노이즈 효과를 사용해서 종이가 불타서 사라지는 듯한 연출을 하고 싶어. 

아이템2가 실행되면 시각 효과와 함께 ice-freezing.MP3를 사용한다. 얼음이 생길때, 사라질때 사용


💻 1. 고도 엔진 디졸브 셰이더 코드새로운 셰이더(Shader) 파일을 생성하고 아래 코드를 작성하세요. (2D의 경우 shader_type canvas_item;, 3D의 경우 shader_type spatial;로 첫 줄을 설정합니다.)glslshader_type canvas_item; // 3D 오브젝트라면 'spatial'로 변경하세요.

// 유니폼 변수 (인스펙터 창에서 조절 가능)
uniform sampler2D noise_texture : hint_default_white;
uniform float dissolve_value : hint_range(0.0, 1.0) = 0.0;
uniform vec4 burn_color : source_color = vec4(1.0, 0.3, 0.0, 1.0);
uniform float burn_size : hint_range(0.0, 0.2) = 0.05;

void fragment() {
    // 기존 오브젝트의 텍스처 색상과 알파값 가져오기
    vec4 main_texture = texture(TEXTURE, UV); // 3D는 TEXTURE 대신 BASE_NEXTURE 등을 사용하거나 생략
    
    // 노이즈 텍스처의 밝기(R 채널) 값 읽기
    float noise_value = texture(noise_texture, UV).r;
    
    // 디졸브 수치보다 노이즈 값이 낮으면 해당 픽셀을 삭제
    if (noise_value < dissolve_value) {
        discard;
    }
    
    // 불타는 테두리(Edge Glow) 효과 계산
    if (noise_value < dissolve_value + burn_size) {
        // 테두리 영역을 기존 색상과 불타는 색상으로 혼합
        main_texture.rgb = burn_color.rgb;
    }
    
    COLOR = main_texture;
}
코드를 사용할 때는 주의가 필요합니다.🛠️ 2. 에디터 설정 방법머티리얼 적용: 효과를 줄 노드(Sprite2D 또는 MeshInstance3D)를 선택합니다.인스펙터 창: Material 항목에서 New ShaderMaterial을 생성합니다.셰이더 로드: 방금 만든 ShaderMaterial 안에 위 코드가 담긴 셰이더 파일을 넣습니다.텍스처 지정: 셰이더 파라미터(Shader Parameters) 목록이 나타나면 Noise Texture 항목에 New FastNoiseLite 또는 New NoiseTexture2D를 생성하여 할당합니다.값 테스트: Dissolve Value 슬라이더를 0.0에서 1.0으로 움직이며 오브젝트가 사라지는지 확인합니다.🎬 3. 스크립트(GDScript)로 애니메이션 구현하기몬스터가 죽거나 아이템이 나타날 때 이 효과를 코드로 제어하려면 트윈(Tween) 기능을 사용하는 것이 가장 깔끔합니다.gdscriptextends Sprite2D

func start_dissolve():
	# 트윈 객체 생성
	var tween = create_tween()
	
	# 머티리얼의 'dissolve_value' 파라미터를 1.0초 동안 0.0에서 1.0으로 변화시킵니다.
	tween.tween_property(material, "shader_parameter/dissolve_value", 1.0, 1.0)
	
	# 애니메이션이 끝나면 오브젝트를 삭제합니다.
	tween.tween_callback(queue_free)



## 아이템3
item3=item.move.current : 사용자가 선택한 고양이가 해당 색상 hole로 머리가 y축으로 살짝 들어올려져서 hole까지 이동하여 빨려들어감
타임 스톱은 연속 사용시 10초씩 누적 해줘
-  arrow.png를 고양이 머리쪽을 가르키며 선택 하도록 유도
- arrow.png 는 로테이션 하지 않고 정방행으로 가르킴 
- arrow는 고양이 머리가 위치한 한칸 위 블럭 약간 아랫쪽에 위치
- 화살표나 고양이를 터치하면 고양이 색상과 동일한 구멍으로 고양이를 클리어 시키면 됨

arrow.png는 사이즈 애니메이션으로 뽀잉뽀잉 움직임을 주면서 야간 위, 아래로 동동거리는 모션을 준다.



item1 Toggle: off = item1, on = itme1_cancle
itme3 Toggle: off = item3, on = item3_cancle


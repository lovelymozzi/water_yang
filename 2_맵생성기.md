# 맵 자동생성 스펙

2026-08-12 구현. **맵 생성에 관해서는 이 문서가 단일 기준이다.** 이동 규칙은 `1_움직임고찰.md`,
프로젝트 전반 규율은 `DEVELOPMENT_RULES.md` 를 본다.

## 0. 왜 역설계인가

퍼즐의 재미는 **A가 나가려면 B가 먼저 나가야 하고, B는 C가 먼저 나가야 한다**는 순서 강제에서
나온다. 정방향으로 무작위 배치를 뿌리고 풀리는지 확인하는 방식은 대부분의 시도가 버려지고,
풀이가 있는지도 매번 처음부터 증명해야 한다.

그래서 **다 풀린 상태(고양이 전원 탈출)에서 시작해 고양이를 구멍 옆에 되돌려 놓고 거꾸로 걸어
나오게** 한다. 역주행 한 스텝은 정방향 한 스텝의 역이므로, 생성이 끝난 순간 풀이 수순이 이미
손에 있다. **풀이가 없는 맵이 나올 수 없다.**

## 1. 데드락이 없다는 것의 근거

솔버를 어디까지 돌릴지가 여기에 달려 있으므로 먼저 정리한다.

- **한 칸 이동은 항상 되돌릴 수 있다.** 끝 `e` 를 칸 `c` 로 옮긴 뒤 반대쪽 끝을 방금 비운 칸으로
  옮기면 원래 상태다. 그 칸은 자기가 비운 칸이라 비어 있고, 장애물·구멍일 수 없으며, 되돌아간
  상태는 원래 흡입이 걸리지 않은 상태였으므로 흡입도 새로 걸리지 않는다.
- **흡입은 되돌릴 수 없지만 칸을 비우기만 한다.** 고양이는 서로에게 장애물일 뿐이므로 한 마리가
  사라지는 것은 남은 고양이의 풀이를 절대 없애지 않는다.

⇒ **시작 상태가 풀리면 도달 가능한 모든 상태도 풀린다.** 그래서 솔버는 최소 수순을 구하지 않는다.
고양이 4마리 × 63칸이면 상태공간이 폭발하는데 이 논증이 있으므로 필요도 없다. 난이도는 풀이
길이와 의존 사슬 깊이로 읽는다. 이 논증은 `scripts/level_solver.gd` 머리에도 적어 두었고,
`random_play_probe()` 가 경험적 뒷받침을 한다.

## 2. 규칙 모델

생성기와 솔버는 노드 없는 `PuzzleState` 위에서만 돈다(`CatEntity` 는 FBX·셰이더·스켈레톤을 들고
있어 수만 번 탐색에 쓸 수 없다). 그 모델은 아래를 **정확히** 복제한다.

| 규칙 | 실제 구현 |
| --- | --- |
| 새로 점유하는 칸 조건: 보드 안 + 장애물 아님 + 구멍 아님 + 다른 고양이 아님 + **자기 몸 아님(뒤끝 포함)** | `CatEntity.can_enter()`, `CatEntity._can_slide_into()`, `LevelManager.is_cell_blocked_for()` |
| 전진과 후진의 조건이 같다 → **원자 이동은 "두 끝 중 하나를 인접한 빈 칸으로" 하나뿐** | 위 두 함수가 같은 집합인 것 |
| 흡입은 **방금 움직인 끝**(리드)이 짝 색 구멍과 4방향 인접할 때만 걸린다. 반대쪽 끝이 스친 것은 흡입이 아니다. 모델의 한 수는 "끝 하나를 잡아 한 칸 끈다"이므로 움직인 끝이 곧 리드다 | `CatEntity._try_begin_absorb()`, `PuzzleState._resolve_absorption()` |
| 구멍은 경로가 아니다 | `LevelManager.is_cell_blocked_for()` |
| 색 짝은 같은 `color_id` 만. `-1` 은 와일드카드 | `LevelManager.color_ids_pair()` |
| 시작부터 짝 구멍에 인접한 배치는 레벨 설계 오류 | `DEVELOPMENT_RULES.md` 구멍 절 |

이동을 끝 인덱스(0/1)가 아니라 **칸**으로 표현한다(`{cat_id, from_end_cell, to_cell}`).
실제 게임은 `_flip_lead()` 가 `body_cells` 를 뒤집으므로 인덱스로 기록하면 재생 때 어긋난다.

`_reverse_target()` 의 벽 슬라이드 무작위성은 모델에 넣지 않는다. 재생은 항상 "잡은 끝을 빈
칸으로"만 하므로(자기 몸을 가리키지 않으므로) 후진 슬라이드 경로를 타지 않는다. 그래서 재생이
결정적이다.

**모델이 실제와 어긋나는 것이 이 프로젝트의 단골 실패 모드다.** 그래서 두 겹으로 대조한다 —
`tests/puzzle_state_parity_check.gd`(칸 판정·인접 조회·이동 집합·흡입 시점을 보드 전 칸에서 비교)와
`tests/generator_replay_check.gd`(기록된 풀이를 실제 게임 코드로 눌러 재생).

## 3. 역설계 절차 (`scripts/map_generator.gd`)

탈출 순서와 고양이 인덱스가 같다. σ_0 이 먼저 나가고 σ_{n-1} 이 마지막이다.

정방향에서 σ_k 가 움직일 때 판 위에는 σ_{k+1}..σ_{n-1} 이 **시작 자리 그대로** 있고
σ_0..σ_{k-1} 은 이미 사라졌다. 그래서 역설계는 `k = n-1` 부터 `0` 까지 내려간다. σ_k 를 되돌릴 때
이미 배치된 σ_{k+1}.. 의 시작 몸이 곧 그 시점의 정적 장애물이 되어 정방향 상황과 정확히 일치한다.

**Phase 0 — 구멍.** `cat_count + 안쪽 레이어 수` 개를 서로 체비셰프 거리 2 이상으로 놓고 색을 하나씩 준다.
붙여 놓으면 한 고양이가 지나가다 옆 구멍에 걸리기 쉬워 아래 가드가 대부분의 칸을 거부한다.
`cat_count + 안쪽 레이어 수`가 `LevelManager.pair_colors` 크기를 넘으면 색이 겹치는데, 그러면 두 고양이가 서로의
구멍을 쓸 수 있어 순서 강제가 약해진다. 기본값(4마리 / 4색)은 겹치지 않는다.

**중첩 고양이.** 마릿수가 아니라 **고양이당 확률**(`nested_two_chance`, 얼음 확률과 같은 결)이다 —
판마다 고양이 수가 다르므로 상수 마릿수는 3마리 판에서 과하고 6마리 판에서 밋밋해진다. 주사위는
구멍을 놓기 전에 한 번에 굴려 그 판의 중첩 마릿수를 정한다(구멍 수가 거기에 걸린다).
확률이 0보다 크면 설계상 첫 탈출 대상은 100% 중첩한다. 나머지 고양이의 확률은 그 한 마리를
반영해 낮추므로 판 전체 기대 비율은 입력값 그대로다(`0.7`이면 10마리 중 약 7마리). 최종 기록
풀이에서 실제로 먼저 탈출한 고양이도 중첩인지 저장 직전에 검증한다.

생성 방식은 사용자가 둘 중 하나를 고른다.

- **2중첩만**(`nested_mode=two`) — 선택된 중첩 고양이는 모두 겉+안 두 겹이다.
- **2+3중첩 혼합**(`nested_mode=mixed`) — 선택된 중첩 고양이 안에서 2중첩과 3중첩을
  마릿수 차이 1 이하로 나눈다. 대상이 홀수면 3중첩이 한 마리 더 많아, 중첩 대상이 한 마리뿐인
  판도 반드시 3중첩 고양이를 포함한다.
  따라서 70%는 “3중첩만 70%”가 아니라 “전체 고양이의 약 70%가 중첩 대상이고, 그 안에서
  2중첩/3중첩이 균등”이라는 뜻이다.

선택된 몸체는 바깥색으로 한 번 탈출한 직후, 같은 칸 배열·다음 색으로 다시
생긴다. 3중첩이면 이 과정이 한 번 더 이어진다. 따라서 각 레이어는 다른 구멍으로 나가야 하며
2중첩은 탈출 이벤트 2번, 3중첩은 3번이 기록된다.
역설계로 만든 바깥 몸체에 이 레이어를 넣은 뒤에는 전체 오토솔버가 처음부터 다시 풀어 실제 수순을
기록한다. 의존 그래프는 같은 물리 몸체를 두 번 세지 않는다.

**Phase 1 — 고양이 한 마리 역주행.**

1. *흡입 순간*: 구멍의 인접 빈 칸을 한쪽 끝으로 삼아 길이 L 의 몸을 빈 칸 위로 뻗는다(꺾여도 된다).
2. *역주행*: 정방향 한 스텝이 "끝 하나를 인접한 빈 칸으로 옮기고 반대쪽 끝을 뗀다"이므로, 그 역은
   **끝 하나를 밖으로 뻗고 반대쪽 끝을 뗀다**다.
   - 뒤끝을 뻗는 경우: `earlier = B'[1..L-1] + [x]`, 정방향 수는 `earlier[0]` → `B'[0]`
   - 앞끝을 뻗는 경우: `earlier = [x] + B'[0..L-2]`, 정방향 수는 `earlier[L-1]` → `B'[L-1]`

   `x` 가 빈 칸이고 `B'` 안에 없으면 그것이 곧 정방향 `can_enter()` 를 만족시킨다.
3. **조기 흡입 가드** — 역주행 중간 상태의 두 끝이 **모든 짝 색 구멍**에 대해 인접하지
   않는지 검사한다(흡입 순간의 자세만 예외). 흡입이 리드 전용으로 바뀐 뒤로는 움직인 끝만
   보면 충분하지만, 일부러 두 끝을 다 본다 — 중간 상태의 아무 끝이나 구멍에 붙어 있으면
   플레이어가 그 끝을 잡는 것만으로 조기 탈출할 수 있어 의도한 순서가 무너진다.
4. **마지막 수 제약** — 역주행 첫 스텝(= 정방향 마지막 수)은 뒤끝만 뻗는다. 흡입은 방금
   움직인 끝에서만 걸리므로, 마지막 수는 반드시 트리거 끝(cells[0])을 구멍 옆으로 옮기는
   수여야 한다.
5. *의존성 유도*: 뻗는 칸이 뒤 순번 고양이의 경로 위면 가중치를 준다. 그리고 완성된 산책 중에서
   **시작 몸이 뒤 순번 고양이의 경로를 가장 많이 막는 것**을 고른다. 걸음 수는 동점을 가르는 데만
   쓴다. 의존 간선은 시작 몸이 남의 길을 막을 때만 생기므로 긴 산책보다 이쪽이 중요하다.

**Phase 2 — 장애물 사후 주입.** 풀이 전 구간에서 어느 고양이든 밟은 칸과 구멍 칸을 모아
`touched` 를 만들고 **그 밖의 칸에만** 놓는다. 인접한 칸은 사각형으로 묶어 `block_size` 한 개로
낸다(칸마다 노드를 만들지 않는다는 기존 배치 규칙).

> **장애물만 사후 주입이 안전한 이유:** 장애물은 "칸을 뺏는" 순수 감산이다. 기록된 풀이는 고정된
> 칸 순서이므로 그 칸을 하나도 건드리지 않으면 수순이 그대로 유효하고, 1절 논증에 의해 데드락도
> 없다. **상태를 바꾸는 기믹에는 이 논증이 성립하지 않는다.**

**Phase 3 — 검증과 재시도.** 아래를 모두 통과할 때까지 시드를 옮겨 재시도한다(기본 80회).
시작 배치 규칙 / 장애물 주입 후에도 기록된 풀이가 재생됨 / 오토솔버가 기록과 무관하게 스스로
풀어냄 / 의존성 비순환 / 사슬 깊이 ≥ `min_chain_depth` / 난이도 탐색 완료.

`random_play_probe()`는 역설계가 만든 데드락 없음을 표본으로 확인하는 **회귀 검사**다.
맵 생성 시도마다 돌리지 않고 `tests/generator_check.gd`에서 따로 돌린다. 난이도도
`squeeze` 전후로 중복 측정하지 않고 **최종 판에서 한 번만** 채점한다.

최종 출력 직전과 JSON 로드 직후에는 고양이·구멍·기록 풀이가 한 번도 쓰지 않는 외곽 행·열을
잘라 낸다. 외곽에만 있는 장애물은 보드 경계가 같은 차단 역할을 하므로 함께 제거하고, 경계에 걸친 장애물만 남은 보드에 맞게 자른다. 기록 풀이의 출발/도착 칸까지 경계에
포함하므로 보드만 작아지고 풀이는 동일하다.

## 3.5. 난이도 채점 (`LevelSolver.difficulty_breakdown()`)

**난이도의 단일 기준은 이 값 하나다.** 생성기의 소프트 게이트(`min_difficulty_score`),
배치 생성기의 스테이지 배당, 어드민의 난이도 곡선이 전부 이 함수를 쓴다.

    costs = LevelSolver.early_escape_costs(state, 0, 8)   # 중첩 내부까지 모든 탈출의 "손을 옮기는 횟수"
    score = round(Σ costs[i] · 0.7^i · 10) + 공간압박(0~15) + 긴몸제약(0~15)

- **왜 드래그 수인가.** 게임은 한 번 잡으면 몇 칸이든 끌 수 있으므로, 체감 난이도는 이동
  칸 수가 아니라 **다른 고양이로 손을 옮기는 횟수**다.
- **왜 감쇠하는가.** 한 마리가 빠지면 몸 + 구멍만큼 칸이 한꺼번에 열린다(실측: 빈 칸
  3 → 10 → 17 → 24 → 32). 같은 드래그 수라도 초반 것이 훨씬 어렵다.
- **공간과 긴 몸 보정.** 빈 칸 비율이 20%보다 낮을수록 최대 15점, 몸 길이 5칸 초과분은
  합산 최대 15점을 더한다. 같은 손 전환 횟수라도 움직일 자리가 거의 없거나 긴 몸을
  다뤄야 하면 체감 난이도가 더 높기 때문이다.
- **왜 사슬 깊이를 안 쓰는가.** `chain_depth` 는 "나머지 고양이가 전원 시작 자리에 얼어
  있다"는 가정에서 재므로 실제 플레이와 무관하다. 생성된 52스테이지 기준 실측 총 드래그
  수와의 상관이 **0.24** 로 사실상 잡음이다. 그래서 배치 생성기의 기본값을 `chain=0`
  (검사 안 함)으로 내렸다.
- **상한도 함께 감쇠한다.** 상한은 곧 0-1 BFS 의 깊이라 비용이 지수로 붙는데, 5번째 탈출의
  1드래그 차이는 점수에 0.2 밖에 기여하지 않는다. 상한 8 → 6 → 4 → 3 → 2 → 1.
- **전역 비용 단계로 찾는다.** 예전처럼 고양이 1마리의 상한 8 탐색을 끝낸 뒤
  다음 고양이를 보면, 뒷번 고양이가 0비용으로 바로 나갈 수 있어도 앞의 큰 탐색을
  먼저 소비한다. 지금은 모든 고양이의 0비용을 먼저 확인한 뒤 1, 2, … 순으로 올린다.
- **예산 초과는 난이도가 아니라 `unknown`이다.** 노드 예산을 넘긴 탐색을 "쉽은 탈출이 없다"로
  판정하지 않고 생성 실패로 분리한다. 한 번의 채점은 고양이별로 예산을 새로 받지 않고 하나의
  공유 예산을 쓴다.

참고 값(옛 배치): 55스테이지 88, 가장 어려웠던 36스테이지 121, 중앙값 60 근처.

### 하드 강제가 아니라 소프트 채점

높은 기준을 개별 조건으로 강제하면 버리는 맵만 늘고 체감 난이도는 안 오른다. 그래서
`chain` / `dep_slack` 은 기본적으로 끄고, **점수 하한(`score`) 하나만 소프트 게이트로**
둔다. 같은 파라미터로도 쉬운 판과 어려운 판이 섞여 나오는데, 그 편차는 버리지 않는다.

**난이도 순 배당은 생성 때 하지 않는다.** 게임이 완성된 뒤 전체 맵을 놓고 한 번에 하는
별도 작업이다. 생성은 **한 개 만들 때마다 바로 저장한다** — 중간에 끊겨도 그때까지 만든
것이 남아야 한다. 배당의 재료로 `stats.difficulty_score` 를 파일마다 적어 두고, 어드민의
난이도 곡선이 같은 값을 읽는다.

첫 탈출까지 치우는 수는 **1로 고정**한다. 바로 탈출 가능한 판은 제외하면서, 2~3 이상의 하한에서
벽 후보 부족과 중첩 솔버 재실행이 반복되던 생성 부하를 줄인다. 생성기에 이 조건을 만들어 내는
장치는 `_plant_first_escape_walls()`이며, 스테이지 어드민과 배치 생성기에서는 값을 바꿀 수 없다.

### 몸 길이는 후반 난이도를 깎는다

탈출 1회가 여는 칸 = **몸 길이 + 1**. 길이 12 짜리는 한 마리가 나갈 때마다 13칸을 연다.
그래서 기본값을 `len_min=3 / len_max=6 / pack=0.7` 로 내렸다 — 짧게 여럿이 탈출당 열리는
칸이 적어 후반이 덜 헐렁하다. 길게 잡으면 시작 판은 꽉 차 보이지만 두 번째 탈출부터
허허벌판이 된다.


## 4. 의존성 그래프 (`scripts/dependency_graph.gd`)

간선 `u → v` 는 **"u가 탈출하려면 v가 먼저 나가야 한다"** 는 뜻이다. 난이도 지표는
`longest_chain_depth()` 이며 **3 이면 A → B → C** 가 강제된다.

> ⚠ **난이도 지표로 쓰지 않는다.** 3.5절 참조 — 실측 난이도와 상관 0.24 다. 순환 검출(아래)과
> "이 맵에 의존이 있기는 한가"를 보는 진단용으로만 남긴다.

**순환은 곧 소프트락이다.** 둘 다 상대가 먼저 나가기를 기다리므로 아무도 나갈 수 없다.
순환이 나온 맵은 즉시 버린다.

간선은 두 가지 방식으로 만들어 대조한다.

- *의도 간선*: 역설계 Phase 1-5 의 가중치가 만든 것.
- *실측 간선*: `LevelSolver.build_dependency_graph()` 가 만든 것. σ_k 가 움직이는 시점의 판에서
  σ_j 하나만 제자리로 되돌려 놓았을 때 σ_k 가 혼자 못 나가면 간선 σ_k → σ_j 다. 한 마리만
  움직이므로 상태공간이 그 고양이의 몸 배치뿐이라 싸다. **A → B → C 가 진짜 강제된다는 근거는
  이쪽이다.**

## 5. 오토솔버 (`scripts/level_solver.gd`)

- `solve()` — 최소 수순이 아니라 **풀이 하나**를 찾는다. 이진 힙 우선순위 큐 + 정규화된 상태 해시
  전치 테이블 + `f = g + 4h` (h = 남은 고양이가 짝 구멍 옆칸까지 가야 하는 맨해튼 거리의 합).
  기록된 풀이가 존재하므로 실제 탐색 노드는 20 이하로 끝난다.
- `can_escape_alone()` — 다른 고양이를 고정한 채 한 마리의 몸 배치만 BFS. 의존성 실측용.
- `random_play_probe()` — 무작위로 몇 수 둔 뒤에도 풀리는지. 1절 논증의 경험적 뒷받침.
- `first_escape_witness()` — 모든 고양이를 공통 비용 단계로 검사하는 0-1 BFS. 채점 핵심이며
  노드 예산 초과 여부를 `complete` 로 호출자에게 전달한다.

`squeeze` 단계는 현재 유효한 풀이가 밟지 않는 칸은 솔버 없이 바로 막는다. 풀이가 실제로
지나는 칸에서만 대체 풀이를 탐색하며, 대체 풀이를 찾으면 다음 후보부터 그 경로를 기준으로 한다.

배치 생성기의 `attempts`는 조건 완화 사다리 전체의 **실제 총 시도 예산**이다.
`MapGenerator.generate()` 내부가 시도마다 시드를 이동하므로 바깥의 추가 seed-retry를 두지 않는다. 따라서
`attempts=120`은 120회이지 완화 단계·중복 재시도에 겹쳐 최대 720회로 불어나지 않는다.

상태 해시는 몸을 뒤집은 것을 같은 상태로 정규화한다. 안 하면 같은 상태를 두 번 탐색한다.

## 6. 꺾인 시작 몸 (`CatEntity.initial_body_cells`)

역설계가 내는 자세는 꺾여 있으므로 `grid_pos` + `facing_name` + `initial_length` 의 직선 몸으로는
심을 수 없다. `initial_body_cells` 에 4방향 인접·자기교차 없는 2칸 이상의 경로를 주면 그것을
그대로 쓰고, `grid_pos` / `facing_name` / `initial_length` 를 거기서 파생시킨다(세터 재진입을 막는
`_applying_initial_body` 가드가 붙어 있다).

**비어 있거나 유효하지 않으면 기존 직선 로직으로 폴백한다.** 손 배치와 기존 회귀 검사 3종은 그
경로를 그대로 쓰므로 영향을 받지 않는다.

포즈 쪽은 손댈 것이 없었다. `_update_visual_pose()` 는 `body_cells` 폴리라인의 호 길이로 본을
놓으므로 **이동 중 꺾인 몸과 완전히 같은 코드 경로**다.

## 7. 출력

| 방식 | 함수 |
| --- | --- |
| 에디터 버튼으로 현재 씬 채우기 | `MainScene/MapGenerator` 노드의 `Generate Level` |
| 레벨별 씬 파일 | `Save Level Files` → `scenes/levels/level_<seed>.tscn` |
| JSON (배치 + 풀이 수순 + 의존성) | `Save Level Files` → `resources/levels/level_<seed>.json` |

배치는 전부 템플릿 인스턴스로만 한다 — 고양이 `scenes/cat_entity.tscn`, 장애물
`scenes/obstacle_block.tscn`, 구멍 `HoleMarker`. 노드 Transform 을 손으로 옮기지 않고 `grid_pos`
(고양이는 `initial_body_cells`)만 지정하면 `LevelManager` 가 `grid_to_world()` 로 맞춘다.

고양이 템플릿은 `tint_from_pair_color = true` 다. 손으로 맞춘 색과 짝 색이 어긋나면 플레이어가
짝을 볼 수 없기 때문이다.

## 7.5. 스테이지 배치 (`scripts/stage_arranger.gd`)

생성은 난이도 순서를 잡지 않는다. 맵이 다 모인 뒤에 어드민(**배치** 절)에서 순서만 다시 매긴다.
파일 내용은 한 바이트도 안 바뀌고 이름만 `stage_001` 부터 다시 붙는다(순번의 구멍도 이때 메워진다).

**난이도 리듬.** 점수로 줄 세운 뒤 난이도 **띠**로 자르고, 한 주기의 패턴대로 자리마다 어느 띠에서
뽑을지 정한다. 띠 안은 오름차순이라 추세는 우상향이다. 단조 증가로 깔면 후반이 전부 고비라 지치는데,
이렇게 하면 매 주기에 숨 고르기가 들어가면서 평균은 계속 올라간다. 띠 크기는 그 난이도가 주기에서
차지하는 비율 그대로라 어떤 패턴을 넣어도 판이 남거나 모자라지 않는다.

| 패턴 | 주기 | 띠 배분 |
| --- | --- | --- |
| 퐁당퐁당 | 쉬움·어려움 | 절반씩 (진폭 1) |
| 6주기 | 쉬움·쉬움·어려움·쉬움·쉬움·매우어려움 | 2/3 · 1/6 · 1/6 (큰 산이 6판마다) |
| 30레벨 평탄화 + 6주기 | 1~30은 쉬움, 31부터 쉬움·쉬움·어려움·쉬움·쉬움·매우어려움 | 초반 30판은 쉬운 띠 안에서 오름차순, 이후 6주기 |

패턴은 `StageArranger.PATTERNS` 에 한 줄 추가하면 어드민 드롭다운에 그대로 뜬다.

**기믹 첫 등장과 연습 구간.** 기믹별로 "이 스테이지보다 앞에는 안 나온다"를 정하고, **첫 등장
자리부터 이어지는 `TEACH_FOLLOW_UP + 1` 판(기본 4판)은 그 기믹이 든 판으로만 채운다** — 한 번
보고 지나가면 규칙을 못 배운다. 이 제약이 난이도 리듬보다 **우선한다**. 첫 등장을 1(= 제한 없음)로
두면 연습 구간도 없다. 두 기믹의 연습 구간이 겹치면 먼저 시작한 쪽이 그 자리를 갖는다.

해당 기믹 판이 모자라면 연습 구간이 짧아지고(자리를 비우지는 않는다), 실제 첫 등장과 채운 판 수가
결과에 찍힌다.

검증은 `tests/stage_arrange_check.gd`(순서 규칙 + 이름 바꿔치기).

## 8. 사람이 직접 플레이해 보기

재미 판단은 자동 검증이 대신할 수 없다. 특정 시드를 직접 플레이하는 방법이다.

1. `scenes/main_scene.tscn` 을 열고 **`MapGenerator`** 노드를 고른다.
2. `Generate On Play` 를 켜고 `Generator Seed` 에 원하는 시드를 넣는다.
3. F5 로 실행한다. 시드가 만든 판이 올라오고, 콘솔에 탈출 순서·의존 사슬·풀이 길이가 찍힌다.
4. 시드만 바꿔 가며 2~3을 반복한다.

`Generate On Play` 는 실행할 때 판을 갈아 끼우는 것이므로 **씬 파일의 손 배치를 덮어쓰지
않는다.** 그래서 시드를 계속 바꿔 봐도 원래 테스트 레벨이 남는다. 손 배치 레벨을 다시
플레이하려면 이 값을 끈다.

> **`--script` 로 띄운 실행에서는 자동 생성을 하지 않는다.** 이 값은 씬 파일에 저장되므로,
> 켜 둔 채로 두면 main_scene 을 읽는 모든 하네스가 손 배치 대신 생성 레벨을 받아
> `movement_check` / `hole_check` / `obstacle_check` 가 통째로 깨진다. F5/F6 실제 플레이에는
> `--script` 인자가 없다는 것으로 구분한다.

에디터에서 배치만 눈으로 보려면 `Generate Level` 버튼을 쓴다. 이건 **현재 씬을 실제로
고치므로** 저장하면 손 배치가 사라진다. 남겨 두려면 `Save Level Files` 로
`scenes/levels/level_<seed>.tscn` 을 따로 뽑고 그 씬을 F6 으로 실행한다.

## 9. 검증

```bash
/Applications/Godot.app/Contents/MacOS/Godot --headless --script tests/puzzle_state_parity_check.gd
/Applications/Godot.app/Contents/MacOS/Godot --headless --script tests/generator_check.gd
/Applications/Godot.app/Contents/MacOS/Godot --headless --script tests/generator_replay_check.gd
```

- **`puzzle_state_parity_check.gd`** → `PARITY CHECK: PASS`. 모델 대조. 보드 전 칸(테두리 포함
  99칸)의 `can_enter` / `is_cell_blocked_for`, 5가지 색의 인접 구멍 조회, 3개 배치의 이동 집합,
  흡입 시점, 꺾인 몸 심기와 직선 폴백.
- **`generator_check.gd`** → `GENERATOR CHECK: PASS`. 시드 20개. 시작 배치 규칙, 기록된 풀이
  재생, 장애물이 풀이 칸을 침범하지 않음, 오토솔버 독립 풀이, 사슬 깊이와 각 간선의 실제 강제
  여부, 무작위 플레이, 같은 시드가 같은 맵을 내는 결정성, JSON 왕복.
- **`generator_replay_check.gd`** → `REPLAY CHECK: PASS`. **오토솔버 검증의 최종 근거.** 생성
  레벨을 실제 `main_scene` 에 심고 `begin_drag()` + `request_path_to()` + `advance()` 로 눌러
  재생한다. 매 수마다 게임의 `body_cells` 와 모델의 몸을 대조하고, 전원 흡입 + `level_cleared`
  발생을 확인한다.

기존 3종(`movement_check` / `hole_check` / `obstacle_check`)도 함께 돌려 직선 폴백이 살아 있는지
확인한다. 하네스는 서로 독립이어야 한다.

### 눈으로 확인

```bash
/Applications/Godot.app/Contents/MacOS/Godot --script tests/capture_generated.gd
```

`user://shots_generated/*.png` 로 생성 레벨 3개를 남긴다(헤드리스가 아니어야 렌더된다).
에디터에서는 `MapGenerator` 노드의 `Generate Level` 버튼으로 같은 것을 본다. 볼 것은 **꺾인 시작
몸의 본 포즈** — 얼굴이 위를 보는지, 코너에서 몸이 옆 칸으로 밀리지 않는지, 생성 고양이가 손
배치 고양이와 같아 보이는지.

## 10. 현재 수치와 한계

Godot 4.7.2 프로파일(`tests/generator_profile.gd`, 고양이 5 / 7×9 / 몸 3~6 /
조합당 10회) 기준으로, 기본 생성은 8,407ms → 243ms, `squeeze` 생성은
47,075ms → 1,708ms로 줄었다. 각각 약 35배, 28배 개선이다. 성능 수치는 해당 프로파일
조합의 회귀 기준이며, 스테이지 생성률을 보장하는 수치는 아니다.

- **기록된 풀이는 최단이 아니다.** 역주행이 무작위 산책이라 32수를 걷지만 오토솔버는 13수로
  끝낸다. 플레이어가 느끼는 난이도는 후자에 가깝다. 난이도를 올리려면 산책 길이를 늘리는 것보다
  **역주행이 구멍에서 멀어지는 방향을 선호하게** 만드는 것이 맞다.
- **장애물은 게임에서 아무것도 그리지 않는다.** 기존 프로젝트 상태 그대로이며(에셋 대기 중),
  그래서 캡처 이미지에서는 잠긴 칸이 보통 타일과 구분되지 않는다. 에디터에서는 반투명 덩어리로 보인다.
- 사슬 깊이 4 이상을 요구하면 재시도가 급격히 늘어난다. 고양이 수를 함께 올려야 한다.
- 와일드카드 구멍(`color_id = -1`)은 만들지 않는다. 모든 고양이가 그 주변을 피해야 해서 조기 흡입
  가드가 역주행을 크게 막는다.

## 11. 기믹 (아직 없음)

**⚠️ 기믹을 만들 때 반드시 지킬 것: 기믹은 역설계 루프 안에서 함께 되돌려야 한다.**

역설계가 끝난 맵에 기믹을 사후 주입하면 기록된 풀이가 무효가 되어 풀이 없는 맵이 될 수 있다.
장애물이 예외인 이유는 3절 Phase 2 의 논증(순수 감산)이며, 상태를 바꾸는 기믹에는 그 논증이
성립하지 않는다.

지금 기믹이 하나도 없으므로 확장 지점만 비워 두었다.

- `PuzzleState.gimmicks` — 칸별 규칙 표.
- `PuzzleState._apply_gimmicks_on_commit()` — 이동 커밋 직후, 흡입 판정 **전에** 불린다.
  실제 게임에서는 `CatEntity._finish_step()` 의 같은 위치에 대응하는 것을 넣어야 한다.
- 기믹 규칙을 넣으면 `MapGenerator._random_reverse_walk()` 에 그 규칙의 **역**을 함께 넣어야
  한다. 한쪽만 넣으면 생성기가 만든 풀이가 게임에서 재생되지 않고, `generator_replay_check` 가
  즉시 그걸 잡아낸다.






구멍은 고양이와 1:1 매치로 고양이의 마지막이 빨려들어갈때 함께 수축되어 맵에서 사라져야 하고
구멍이 사라진 자리는 평범한 타일이 되므로 고양이가 이동이 가능함.
맵제작이 이부분을 반영하게 되면 같은 맵에서도 난이도가 굉장히 쉬워지게 되고 반대로 말하면 이 부분을 이용하면 맵을 더 의존성이 심하게 구멍으로 막아서 하나씩 순차적으로 풀어야만 다음것이 풀리는 의존성의 키가 될수 있음
의존성을 연구해서 더 높일수 있는 방안을 연구해줘.
한마리가 빠져나가면 난이도가 급감하기 때문에 현재로서는 첫탈출의 난이도를 보정할수 있는 조건이 필요함.
첫탈출 까지의 치워야 하는 무브수를 최소제한을 두어서, 최소 3수, 4수이상 무브해야 한마리가 빠질수 있게 해줘.



어차피 맵은 굉장히 많아야 하기 때문에
아주 높은 기준을 강제하여 버리는 맵이 많아지는것은 불필요한 작업임.

그래서 지금 하는
의존성이 최대한 높아지게 생성할수 있는 방식을 찾아내고

맵을 생성할때 특정 조건을 하드하게 강제하지말고 소프트하게하여
최소 난이도점수를 통과하면 패스하는 방식으로 제작함.

그 과정에서 쉬운맵도 우연히 매우어려운 맵도 생성이 될 것인데
의존성 체인을 점수화 해서 사후에 난이도를 배당함.


맵 제네레이트가 제대로 된 어려운 맵을 생성하지 못하는데



레온아웃이나 게코아웃의 경우에는  빈공간이 거의 없음.
1번 탈출부터 의존성이 있어서 풀이가 쉽지 않게 만들어져있음
1번이 탈출한 공간을 거의 100% 효율로 활용하지 않으면 2번 탈출이 불가하고
1번과 2번이 탈출한 공간을 역시나 100% 활용하지 않으면 3번 탈출이 불가함.
이것을 로지컬하게 생각하면
1번 탈출가능 고양이의 경우에는 바로탈출을 막는정도의 로직이 있지만
문제는 2번 고양이의 탈출에 결정적인 역할을 하는 고양이들을 모두 치워야 탈출길이 열리는데 이 고양이들을 치우는것이 매우 어렵고 의존성이 강하게 연결되는 구조라, 다른 고양이들을 최대한 치워서 탈출로를 1칸의 여유도 없이 딱 만들어내고
그 길로 탈출을 하는 구조라 해법이 난해한 구조임.
이런 맵을 만들어야 하는데
지금의 로직으로는 이런맵을 만들수가 없음.

어차피 후반부에는 많은 고양이들이 빠져나간 뒤라 공간이 여유로워지기 때문에
구조적으로 고양이만으로 어렵게 만들수 없고, 기믹추가를 통해 어려움을 강제로 부여하는 조건을 추가하는것으로
난이도를 보정하게 될것이라

맵 제작 당시부터 후반고양이의 탈출을 로지컬하게 만드는데 시간낭비하지말고
후반은 간략한 의존성과 탈출구로부터 멀리 떨어지는 정도로 가볍게 설계하고
초반에 탈출시키는 n마리의 고양이 탈출 수순에 대해서만 어렵게 만드는것이 맵제작의 목표.

예를들어 8마리의 고양이가 있다면 5마리를 대충 배치하고
3마리의 해가 나오는 조건을 어렵게만들기 위해 기존에 배치된 모든 요소를 활용하여
맵을 만드는데 공을 들여서 완성하는것.

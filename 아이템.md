# 아이템 보상 정리

## 확정 사항

- 스테이지 클리어 1회당 아이템 보상은 `없음` 또는 `아이템 1개`만 지급한다.
- 보상 아이템은 실제 보유 수량에는 매번 1개씩 누적한다.
- Lobby 보상 연출 큐에서는 같은 아이템을 여러 번 받아도 1개로 간주한다.
- Lobby 보상 연출 큐는 `localStorage`의 `leonout_lobby_item_rewards_v1`에 저장해 웹빌드 리로드 후에도 유지한다.
- Lobby 순차 연출은 서로 다른 아이템이 들어왔을 때만 발생한다.
- Lobby 순차 연출 최대 개수는 item1/item2/item3 각 1개씩, 최대 3개다.
- LevelWin에서 보상 아이템이 없으면 `coin_reward` Group position은 `x:160 y:305`다.
- LevelWin에서 보상 아이템이 있으면 `coin_reward` Group position은 `x:156 y:305`다.
- LevelWin 아이템 Group position:
  - item1: `x:36 y:307`
  - item2: `x:42 y:303`
  - item3: `x:43 y:302`
- 아이템 보상이 있어도 LevelWin의 Continue/광고 버튼은 다음 레벨 진행 흐름을 유지한다.
- 아이템 획득 연출은 유저가 게임을 끝내고 Lobby로 돌아왔을 때 진행한다.

## 원문 요청

LevelWin 팝업에서 스테이지를 클리어하면 인게임에서 사용하는 아이템을 랜덤 보상으로 지급한다.

- 아이템 종류는 `인게임_아이템.md` 기준을 따른다.
- 스테이지 클리어 시 랜덤으로 아이템을 지급한다.
- 현재 맵 밸런스는 `2_맵생성기.md` 기준으로 참고해서 보상 빈도와 아이템 종류를 정리한다.
- `pop_LevelWin`에서 `coin_reward`는 평소 `x:160 y:305` 위치이고, 랜덤 아이템이 지급된 경우 `x:156 y:305` 위치로 이동한다.
- 보상 아이템 위치는 각 아이템 `Group_Reward`의 Group position 값이다.
  - item1: `x:36 y:307`
  - item2: `x:42 y:303`
  - item3: `x:43 y:302`
- 보상 받은 아이템은 홈으로 이동 후 Lobby에서 보상 연출을 진행한다.
- Lobby 보상 연출:
  - item1/item2/item3을 화면 중앙에 잠시 띄운다. 이때 커졌다 작아졌다 하는 "띠용" 모션 적용.
  - 서로 다른 item 보상이 여러 개 쌓여 있으면 순차적으로 1개씩 보여준다.
  - 같은 item을 여러 번 받았을 때 Lobby 연출에서는 1개로 간주한다.
  - `Rays Burst`, `Gold Glow`를 끈다.
  - 아이템을 획득한 레벨 클리어시 continue나 광고보기 버튼을 누르고 다음 레벨로 이어 진행하도록 한다. 아이템 획득 연출은 유저가 게임을 다 끝마치고 로비로 돌아왔을때 진행.
  - 각 아이템 이미지를 배치된 크기에서 50% 줄인 상태로 여러 개 생성한다.
  - 생성된 아이템 이미지를 Level 버튼으로 Trail 연출처럼 이동시킨다.
  - Trail연출시 유선형 곡선을 그리며  Level 버튼으로 들어간다. 
- 추측하지 말고 애매하거나 이해가 안 되면 질문하면서 구현한다.

## 아이템 종류

`인게임_아이템.md` 기준:

| 아이템 | 내부 키 | 효과 |
| --- | --- | --- |
| item1 | `remove` / `item.remove.current` | 장애물 1개 제거 |
| item2 | `timestop` / `item.timestop.current` | 인게임 타이머 10초 정지, 연속 사용 시 10초씩 누적 |
| item3 | `move` / `item.move.current` | 선택한 고양이를 같은 색 구멍으로 이동/클리어 |

기본 지급 수량은 각 3개다. 사용 성공 이벤트가 왔을 때만 1개 차감한다.

## 보상 밸런스

현재 맵 생성 기준은 `2_맵생성기.md`의 수치와 방향을 따랐다.

- 현재 자동 생성 맵은 오토솔버 기준 평균 풀이가 약 13수다.
- 의존 사슬 깊이는 현재 3 정도를 목표로 하고 있다.
- 문서상 초반 난이도를 보정해야 하며, 아이템이 너무 자주 지급되면 장애물 제거/item3 즉시 탈출로 퍼즐 의존성이 쉽게 무너진다.
- 그래서 클리어 보상은 매번 확정 지급이 아니라 낮은 확률 보너스로 둔다.

현재 구현된 확률:

| 결과 | 확률 |
| --- | ---: |
| 보상 없음 | 75% |
| item2 타임스톱 | 12% |
| item1 장애물 제거 | 8% |
| item3 고양이 이동 | 5% |

item2는 시간을 보태지만 퍼즐 구조를 직접 제거하지 않으므로 가장 높게 잡았다. item1은 장애물을 제거하므로 중간, item3은 고양이 1마리를 바로 클리어할 수 있어 가장 낮게 잡았다.

## 구현 완료 내용

수정 파일:

- `godot-shell.html`
- `web/index.html`
- `tests/item_reward_check.mjs`
- 루트 asset 복사: `item1.png`, `item2.png`, `itme3_1.png`

구현 요약:

- 아이템 수량을 `localStorage`의 `leonout_items_v1`에 저장한다.
- 기존 시작 수량은 각 3개로 유지한다.
- 아이템 사용 성공(`itemUsed`) 시 수량을 1개 차감하고 저장한다.
- 스테이지 클리어 보상은 코인/하트 지급과 진행도 기록이 성공한 뒤 랜덤으로 결정한다.
- 보상 아이템이 있으면 해당 아이템 수량을 1개 추가하고 저장한다.
- `pop_LevelWin`에서 보상 아이템이 없는 경우 아이템 보상 레이어는 숨긴다.
- 보상 아이템이 있는 경우 해당 아이템 레이어만 표시한다.
- 보상 아이템이 없는 경우 `coin_reward` Group position을 `x:160 y:305`로 배치한다.
- 단독 코인 보상은 `coin_reward` Group position `x:160 y:305`를 유지하되, 그룹 내부 레이어를 `x:-67` 보정해 코인+수량 묶음이 중앙에 보이게 한다.
- 보상 아이템이 있는 경우 `coin_reward` Group position을 `x:156 y:305`로 배치한다.
- 보상 객체가 있어도 유효한 아이템 키가 아니면 보상 없음으로 취급해 `coin_reward` Group position을 `x:160 y:305`에 배치한다.
- 보상 아이템이 있는 경우 각 아이템 Group position을 `item1 x:36 y:307`, `item2 x:42 y:303`, `item3 x:43 y:302`로 배치한다.
- LevelWin 보상 그룹 안의 `Gold Glow`, `Rays Burst`, 아이템 이미지, 수량 텍스트는 group 기준 상대 좌표로 재배치한다.
- 아이템 보상이 있어도 LevelWin의 Continue/광고 버튼은 다음 레벨 진행 흐름을 유지한다.
- 아이템 보상 연출은 유저가 게임을 끝내고 Lobby로 돌아왔을 때 진행한다.
- Lobby 보상 연출 대기 큐는 `leonout_lobby_item_rewards_v1`에 저장하고, 실제 연출이 시작될 때 소비한다.
- Lobby 진입 시 보상 아이템을 중앙에 잠시 표시하고, 커졌다 작아졌다 하는 모션을 적용한다.
- Lobby 보상 연출은 Lobby 화면이 실제로 표시 가능한 상태가 된 뒤에만 큐를 소비하고 재생한다.
- 서로 다른 보상 아이템이 여러 개 쌓여 있으면 1개씩 순차적으로 표시한다.
- 같은 아이템이 여러 번 쌓여도 Lobby 연출 큐에서는 1개로 합쳐서 표시한다.
- Lobby 연출은 item1/item2/item3 종류별 최대 1개씩, 최대 3개까지 순차 재생한다.
- 중앙 표시 뒤 Glow/Rays 레이어를 끄고, 50% 크기의 아이콘 여러 개를 유선형 곡선 Trail처럼 Level 버튼으로 날린다.
- `web/ui/scene-flow-changes.json`은 FlowDriver 데이터 전환으로 반영되어 소비 처리했다.

## 검증

통과한 검사:

```bash
node tests/item_reward_check.mjs
node tests/economy_transaction_check.mjs
node tests/ui_bare_asset_paths_check.mjs
```

추가로 `web/index.html`의 모듈 스크립트 파싱 검사를 통과했다.

## 남은 확인

프로젝트 규칙상 시각적/브라우저 검증은 사용자가 직접 수행한다.

확인할 장면:

- item 보상 없음일 때 LevelWin에서 아이템 보상이 숨겨지는지
- item 보상 있음일 때 LevelWin에서 코인 보상 위치와 아이템 위치가 의도대로 보이는지
- 홈 이동 후 Lobby에서 중앙 표시, Glow/Rays off, Level 버튼 Trail 연출이 자연스러운지
